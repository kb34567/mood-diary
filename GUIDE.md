# 心情日記 使用說明

給之後的自己看的小教學。網址：https://kb34567.github.io/mood-diary/

---

## 資料存在哪裡

- 日記、照片、心情、記帳都存在 **Firebase（Firestore）雲端資料庫**，免費額度 1GB。
- 「同步碼」是找到你那份資料的鑰匙——同一組碼在任何裝置、任何瀏覽器打開這個網址，套用後都會看到同一份內容。
- 瀏覽器本機（localStorage）也會留一份快取，讓 App 開起來快一點、離線也能看，但**同步碼跟這份快取都會被「清除瀏覽器網站資料」這個動作一起清掉**，資料本身不會丟（雲端還在），但這台裝置要重新輸入同步碼才能接回去。

**去哪裡看/改同步碼**：設定 → ☁️ 雲端同步

---

## 同步碼弄丟了怎麼辦

1. 先檢查有沒有截圖或記在備忘錄裡。
2. 如果有備份檔（見下面「備份 / 還原」），打開備份檔用文字編輯器看，裡面 `syncCode` 那個欄位就是你的碼。
3. 如果真的完全找不到，資料還在 Firebase 專案裡，只是沒有鑰匙打不開——這種情況要進 [Firebase Console](https://console.firebase.google.com/) 直接查 Firestore 資料庫內容（`syncCodes` 這個 collection 底下的文件），把跟你有關的那組碼名稱找出來。可以請 Claude 幫忙做這一步。

**最好的預防方式：定期匯出一次備份檔**（設定 → 💾 完整備份），備份檔裡就包含同步碼，之後翻備份檔就能找回來，不用特地去記。

---

## 空間快滿了怎麼辦

去「設定 → 📊 儲存空間」看目前用量。

- **單一天超過 1MB**（雲端對每一天的資料有這個上限）：那一天的照片可能同步失敗。去那天的日記把畫質最大的一張照片刪掉，或用「🗜️ 重新壓縮照片」把舊照片重新壓縮一次。
- **總用量接近 1GB**：以目前用量（個位數 MB）來看，要走到這一步大概需要好幾年高強度使用，機率很低。真的接近時，選項有：
  1. 升級 Firebase 方案（Blaze，用多少付多少，超過免費額度才開始收費，通常很便宜）
  2. 搬到別的免費資料庫（見下面）

---

## 真的要搬家（換資料庫）怎麼做

因為資料就是單純的 JSON（日記、心情、記帳、分類），不是綁死 Firebase 專屬格式，搬家是可行的，步驟大概是：

1. **先備份**：設定 → 💾 完整備份 → 匯出備份，存好那個 `.json` 檔。
2. **請 Claude 幫忙**：把想換的資料庫告訴 Claude（例如 Supabase、另一個 Firebase 專案、MongoDB Atlas 免費層都可以），請它：
   - 申請/設定新的資料庫服務（跟當初設定 Firebase 一樣，需要你自己開帳號、建專案）
   - 把程式碼裡讀寫資料庫的那幾個函式（`cloudPush`、`cloudSyncEntry`、`cloudLoadAll`）改成新服務的寫法
   - 寫一個小工具把備份檔匯入新資料庫
3. **匯入資料**：新程式上線後，用「設定 → 匯入備份」把備份檔匯進去。
4. 確認資料都在之後，舊的 Firebase 專案可以留著或砍掉都行（不影響新的）。

整個過程大概跟當初「串 Firebase」的工作量差不多，不是很困難，但需要重新走一次帳號申請的流程。

---

## 備份 / 還原怎麼用

**位置**：設定 → 💾 完整備份

- **匯出備份**：把目前所有資料（日記、照片、心情、記帳、同步碼）打包成一個 `.json` 檔下載下來。建議偶爾做一次，存在雲端硬碟或 email 給自己都可以。
- **匯入備份**：選一個之前匯出的 `.json` 檔，會把目前資料整個覆蓋掉（會先跳出確認視窗），如果有連同步碼，也會一起同步回雲端。

---

## 其他常見狀況

**改版後 App 看起來還是舊的畫面**
完全關閉 App（不是切到背景，是真的關掉）再重新打開。網站已經改成「優先讀取最新版本」，正常這樣就會抓到新版；如果還是舊的，才需要去手機設定裡清瀏覽器網站資料（記得清之前先確認同步碼有存好）。

**照片看起來模糊**
設定 → 🗜️ 重新壓縮照片，可以把還沒壓縮過的舊照片處理一次。這個工具會自動跳過已經夠小的照片，可以放心重複按。

**存檔時說「儲存失敗」**
- 如果有連同步碼：資料其實有存到雲端，只是本機瀏覽器空間滿了，訊息會提示你去設定開啟雲端同步。
- 如果還沒連同步碼：本機真的存不進去，去設定建立或輸入同步碼。

**想看某一天的心情圖示是誰、換心情圖片**
設定 → 表情倉庫，可以新增/編輯/刪除心情（刪除前會檢查有沒有被日記用到）。

**想把日記印出來或做成紀念冊**
設定 → 📖 匯出成書，可以選擇要不要隱藏某些心情的內容，會匯出 A5 尺寸的 PDF，一天一頁，內容太多會自動分頁。
